import React from 'react'
import { shallow } from 'enzyme'

import {{componentName}} from '.'

describe('<{{componentName}} />', () => {
  it('renders', () => {
    const wrapper = shallow(<{{componentName}} />)
    expect(wrapper).toMatchSnapshot()
  })

  it('TODO: tests for {{ componentName }}')
})
